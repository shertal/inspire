table:
  name: trip_tag
  schema: public
insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
        - tag_id
        - trip_id
select_permissions:
  - role: public
    permission:
      columns:
        - trip_id
        - tag_id
      filter: {}
update_permissions:
  - role: public
    permission:
      columns:
        - tag_id
        - trip_id
      filter: {}
      check: {}
delete_permissions:
  - role: public
    permission:
      filter: {}
